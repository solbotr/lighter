[pytest]
# Trading suite (`tests/`) is primary; `autobot/tests` is secondary/isolated.
# Collect from both trees so CI and local runs cover the news/sniper suite first.
testpaths = tests autobot/tests
asyncio_mode = auto
# Disable broken web3 pytest plugin (eth_typing version mismatch in this env)
addopts = -p no:pytest_ethereum
