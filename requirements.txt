# Dev / flexible installs: pip install -r requirements.txt
# CI / reproducible installs: prefer requirements.lock (pip freeze after install).
#   pip install -r requirements.lock
#
web3>=6.20.0
python-dotenv>=1.0.0
eth-utils>=2.0.0
eth-abi>=4.0.0
eth-account>=0.10.0
requests>=2.31.0
requests-oauthlib>=1.3
aiohttp>=3.8.0
feedparser>=6.0.0
certifi>=2024.0.0
pytz>=2023.3
python-telegram-bot>=20.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
psutil>=5.9.0
prometheus-client>=0.17.0
websockets>=11.0
# asyncio-contextmanager>=1.0.0  # obsolete; unused with modern asyncio

# Test / CI
pytest>=8.0.0
pytest-asyncio>=0.23.0

# Charts / telegram visuals (used by chart_generator, visual_fill_cards, tests)
Pillow>=10.0.0
matplotlib>=3.7.0

# VPS / remote deploy helpers (scripts/run_vps_cmd.py, _vps_deploy.py)
paramiko>=3.0.0

# The `lighter` SDK (zkLighter) is not always on PyPI — install separately if needed, e.g.:
#   pip install lighter-sdk
#   # or: pip install git+https://github.com/<org>/lighter-python.git
# Paper mode does not require a successful `import lighter`.
